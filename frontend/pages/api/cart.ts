// This file is no longer needed as we're using backend APIs directly
// The cart functionality is now integrated with the backend cart controller
export default function handler() {
  return { message: 'Cart API moved to backend integration' };
}